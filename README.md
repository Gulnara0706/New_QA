# QA Tests

Тесы к заданию на позицию QA Engineer (Middle+/Senior)
```

### Запуск тестов
pytest tests/ — запустить все тесты в папке tests/;

## Переменные окружения

- `BASE_URL` - URL основного приложения

Риски: 

- Сообщение не доходит до сервера (неверный запрос, при сетевых сбоях)

- Сервер обрабатывает запрос некорректно : создается дубликат сообщения, неверно сохраняется сообщение(не к той комнате привязывается или удаляется), сообщения с файлами могут не сохраняться, проблемы с производительностью( долгий ответ сервера), если происходит релиз во время отправки сообщения,  многократное обновление страницы, слишком длинные сообщения, SQL - инъекция, если комната недоступна, можно получить сообщения и отправлять их?)
